name: release

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    name: release disabled for beta
    runs-on: ubuntu-latest
    steps:
      - name: Explain release state
        run: |
          echo "Ryngo beta deploys through the existing Cloud Build / Cloud Run trigger."
          echo "The old pnpm Changesets release flow is disabled because this repo now ships the MVP from mvp/ with npm."
