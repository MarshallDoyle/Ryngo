# .codegraph.yml — minimal starter
#
# Generated by `codegraph init --template minimal`. Most fields are optional;
# the only required key is `schemaVersion`. See spec/config-schema.md for the
# full reference.

schemaVersion: 1

project:
  name: __PROJECT_NAME__

# Files matching any pattern below are skipped entirely. The CLI also applies
# a built-in default ignore set (node_modules, dist, .git, …); entries here
# are merged with those.
ignore:
  - "**/*.generated.*"

# >>> BOUNDARIES_BLOCK >>>
# <<< BOUNDARIES_BLOCK <<<

# >>> ADAPTERS_BLOCK >>>
# <<< ADAPTERS_BLOCK <<<
