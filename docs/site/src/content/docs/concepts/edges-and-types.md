---
title: Edges & types
description: The edge taxonomy — calls, imports, reads, writes, derives-from, routes-to — and how typed edges flow framework-aware information through the graph.
sidebar:
  order: 4
---

> TODO: Open with the core edge kinds and a brief note that adapters can add namespaced kinds (`sql:references-table`, `http:routes-to`). Table format: kind, direction, expected `from`/`to` node kinds, attrs schema, example. End with a "filtering by edge kind" subsection that links to [Filtering](/viewer/filtering/) so readers know how to use this in practice.
