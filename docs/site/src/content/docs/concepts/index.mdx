---
title: Concepts
description: The mental model behind codegraph — the IR, node tiers, edge taxonomy, adapters, the effect system, and the diff algorithm.
sidebar:
  order: 1
---

> TODO: One short paragraph explaining that this section is the mental model and pointing the reader at which page to read for which question. Then a `<CardGrid>` with one card per concept page. The implicit reading order is IR → Nodes & tiers → Edges & types → Adapters → Pure vs effectful → Diff & PR comments, but every page should be entry-pointable.
