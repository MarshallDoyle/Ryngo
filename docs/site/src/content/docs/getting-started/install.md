---
title: Install
description: Install the codegraph CLI via npm, pnpm, yarn, bun, or Homebrew, plus the supported OS/Node matrix and how to verify the install.
sidebar:
  order: 2
---

> TODO: Cover npm/pnpm/yarn/bun global install commands (mirror the README's Installation section), Homebrew once available, and a "from source" path. Add a small OS/Node-version matrix (Node 20+, macOS/Linux/Windows-via-WSL). Finish with the `codegraph --version` verification step and a troubleshooting note for `command not found` (PATH issues).
