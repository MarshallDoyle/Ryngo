---
title: Your first index
description: Run `codegraph index` against a sample repo and walk through the IR file it produces.
sidebar:
  order: 3
---

> TODO: Walk the user through `codegraph index .` on a small example repo (suggest using one of `test-fixtures/` once those exist). Show the expected console output, then the resulting `.codegraph/graph.json` file path. Open the JSON, point out `meta`, `nodes`, `edges`. Cross-link to [The IR](/concepts/ir/) for the full schema.
