# Don't ship local state, build outputs, or test artifacts in the
# image. The build stage rebuilds dist/ inside the container.
node_modules
dist
.ryngo
test/results
*.log
.DS_Store
.env
.env.*
!.env.example
