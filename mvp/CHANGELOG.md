# Ryngo — activity log

Per `AGENTS.md` convention: one line per ship. `date · agent · one
sentence`. Newest at top.

2026-05-11  claude  Compiler-warnings catalog at `mvp/docs/WARNINGS_CATALOG.md`. Twelve categories (perf · async · security · correctness · maintainability · API · framework-specific · test quality · build/deps · docs · accessibility · style), ~120 distinct issue kinds, each tagged with detection cost (🟢 ship-today, 🟡 needs-parser, 🟠 needs-types, 🔴 needs-effects, ⚫ out-of-scope). The seven shipped warnings (`triple-nested-loop`, `nested-loop`, `io-in-loop`, `recursion`, `long-function`, `many-params`, `deep-nesting`) are recapped at the top; the bottom has an opinionated next-20 shipping list ordered by signal-to-noise + cost. Explicit out-of-scope section so we don't drift into pretending to be a type checker / SAST / CVE feed.
2026-05-10  codex/claude  Provenance + confidence labeling across IR and viewer — shipped from disk on Codex's behalf. New `mvp/lib/provenance.js` normalizes every node + edge with a `confidence` field (`confirmed` / `source-syntax` / `framework-inferred` / `heuristic` / `unknown`) and a `source` field tracking which tier produced the entry (parser, adapter, resolver, …). `analyze.js` calls `normalizeProvenance(ir)` before building the quality report. MCP `find_node` surfaces source + confidence per hit. View-model now reports `omittedNodesByKind` + `omittedEdgesByKind` so the inspector can explain what got filtered. Viewer renders the confidence + provenance on file-status chips, function/class headers, and a new `.rfn-node-facts` panel. New `landing/logos.html` (30 SVG mark candidates from the design pass) and `docs/EVALS.md` (Codex's eval plan that seeded claude's eval-tokens benchmark). 14 modified files + 3 new; smoke-tested via `npm run corpus --filter=expressjs/express` (all repos within declared bounds, no anomalies) and a server-boot check (`/api/health` 200, `/api/stats/public` 200, `/` 200).
2026-05-10  claude  Landing Iter 2 shipped (minus newsletter, per user) — three new visual elements on top of Iter 1's foundation. (1) Hero multiplier headline: "843× less context. Same code map." with per-representation percentages beneath (topology 0.12 %, single symbol 0.006 %, focused subgraph 0.28 %). All four numbers baked at deploy time AND hydrated from `/api/stats/public` on load. (2) Hero stats banner under the demo iframe: "56 repos compiled · 293.6k nodes generated · 66.9M tokens compressed · 843× smaller agent context" — real corpus baseline + any live activity from the events warehouse, summed. Same baked+hydrated pattern. (3) `#news` section between Personas and `#ryngo-md`: six dated bullets rendered from baked HTML, swapped at runtime with the latest `/data/news.json`. (4) `#roadmap` "Coming soon" section: three cards (tree-sitter parsers, Go + Rust support, multi-repo aggregation) each with a chip ("in progress" / "next" / "queued"). Nav gained `#news` anchor. Two inline hydration IIFEs at the bottom of index.html: `/api/stats/public` for banner+multiplier, `/data/news.json` for the news list. Both cache: 'no-store' to bypass browser cache and reflect the latest deploy. Baked fallbacks ship in HTML so the page is fully rendered before any JS runs.
2026-05-10  claude  Landing Iter 1.B shipped solo — four new sections in `landing/index.html` + matching CSS. (1) Tagline companion sentence beneath the hero h1 names the category ("deterministic code-to-graph compiler with an MCP server"), the mental model, the four LLM-harness integrations by name, and `Ryngo.md` — 36 words at LiteLLM density. (2) `#ryngo-md` section with three flow cards: live (`npx ryngo-mcp install`), one-off (copy-paste from inspector), persistent (`curl -O Ryngo.md` + `git add` — auto-discovered by Cursor/.cursorrules, Claude Code/CLAUDE.md, Aider/AGENTS.md). Includes a `<details>` with the file format. (3) `#pricing` — single centered Free tile, 6 bullets, $0 chip, "paid tier on the roadmap — every feature is free for everyone." No Enterprise tile (per user). (4) Footer overhauled: three columns (Talk / Build / About) with Cal.com founder link, Discord, hi@ryngo.ai, plus the existing bar with logo + tagline below. Nav gained `Ryngo.md` + `Pricing` anchors. Smoke-tested locally: all four endpoints (`/api/stats/public`, `/api/ryngo-md/download`, `/data/news.json`, `/data/tokens-summary.json`) still respond 200; vite build clean at 538 modules. Codex's other in-flight files (`src/App.jsx`, `src/styles.css`, `logos.html`, `EVALS.md`) untouched.
2026-05-10  claude  Live stats banner backend — `GET /api/stats/public` aggregates `analysis_runs` from the events warehouse (`getLiveStats()` in `lib/events.js` — sums node_count/edge_count/route_count/db_model_count/package_count, distinct repos via join to `repo_submissions`) and overlays a corpus baseline from `landing/data/tokens-summary.json` (`lib/stats-baseline.js`). The slim summary now also carries a `corpusTotals` block (293k nodes · 184k edges · 137k defs · 1,431 routes · 639 db-models · 66.9M raw tokens across 54 repos) so the landing never shows zeros on a cold deploy, even before the events DB has data. 60-second in-memory cache; `cache-control: public, max-age=60` header. Headline block returns banner-ready display strings (`"293.2k"`, `"66.9M"`, `"843× smaller"`) so the client does no math. Smoke-tested cold (no DB → returns baseline) and warm (cache hit returns instantly with same headers).
2026-05-10  claude  Latest-news data source — new `scripts/build-news.js` parses the top 6 lines of `mvp/CHANGELOG.md` (date · agent · body, em-dash splits title from blurb) into `mvp/landing/data/news.json`. Codex's iter-2 landing section fetches this on load, falls back to whatever was baked in at build time. New npm script `build:news`.
2026-05-10  claude  Landing overhaul — 9-change consolidated plan at `mvp/docs/LANDING_OVERHAUL.md`. Synthesizes LiteLLM (`litellmresearch.md`) + Unsloth (`unslothresearch.md`) audits with the user's decisions: Free-only pricing (no Enterprise tile yet), live stats banner powered by Codex's events warehouse (`/api/stats/public` endpoint spec'd — aggregates `analysis_runs` for repos/files/nodes/edges/routes/db-models/packages + corpus baseline fallback), latest-news section auto-parsed from `CHANGELOG.md` (new `scripts/build-news.js`), "coming soon" roadmap cards (tree-sitter swap, Go/Rust support, multi-repo), tagline companion sentence ("Ryngo is a deterministic code-to-graph compiler with an MCP server…"), Cal.com + Discord + email in footer, single newsletter signup, hero multiplier headline ("850× less context, same code map") with the real per-representation ratios beneath. Mascot + testimonials explicitly deferred. Three-iteration ship order with per-change owner suggestions (claude / codex) and acceptance checks. AGENTS.md workstream table updated with each iteration's row.
2026-05-10  claude  Landing — "Three ways to plug Ryngo.md into your agent" section spec'd as a drop-in (`mvp/docs/LANDING_RYNGO_MD.md`) with full HTML + CSS Codex can paste into `landing/index.html` and `landing.css`. Three cards: MCP (live agent tools), copy/paste (one-off chat-UI flow), commit-to-repo (persistent + team-shared via Cursor/.cursorrules, Claude Code/CLAUDE.md, Aider/AGENTS.md). Backend gap closed: `GET /api/ryngo-md?format=raw` and `GET /api/ryngo-md/download` now return the raw Markdown with `Content-Type: text/markdown` + `Content-Disposition: attachment; filename="Ryngo.md"` so card 3's `curl -O` works on day one.
2026-05-10  claude  Token-efficiency benchmark — `mvp/scripts/eval-tokens.js` runs the corpus harness against every entry in `test/corpus.js`, computes raw analyzable-source bytes per repo, then measures each Tier-4 LLM projection (`topology`, `compactJson`, `buildViewModel`, `slice`, `englishSignature`) and rolls up per-language + per-representation medians. First full run on the 54-repo corpus (54/54 OK, 113 s, ~58 M total raw tokens) lands these medians: topology 0.12 % of raw, focused subgraph 0.28 %, view-model 13.9 %, compact IR 60.8 %, english signature 0.006 %. New npm script `eval:tokens`. Outputs three artifacts: `test/results/token-efficiency-latest.{json,md}` + `token-efficiency-history.json` (gitignored, regenerated on demand) and a 10 KB `mvp/landing/data/tokens-summary.json` Codex consumes from the `#evals` section of the landing page. The compact-IR result is the unflattering one — the IR carries explicit edges + types so it's a structural reshape, not byte-level compression; the headline savings live in topology and focused-subgraph instead.
2026-05-10  claude  Phase 11 — `Ryngo.md`, the per-repo manifest. New `mvp/lib/ryngo-md.js` parses/serializes a single root-level markdown file holding `## Comments` (per-node user notes) + `## Suppressions` (warning kinds the user has acknowledged); unknown sections (`## Connections` / `## Expose` / `## Flags`) round-trip verbatim so v2 lands additively. New `mvp/lib/ryngo-md-store.js` reads/writes through `lib/storage.js` and migrates legacy `annotations.md` on first append. `analyze.js` reads `Ryngo.md` from the clone root and filters `def.warnings` against `ir.ryngoManifest.suppressions`, so a dismissed ⚠ stays dismissed across analyses. New endpoints `GET/PUT /api/ryngo-md` + `POST/DELETE /api/ryngo-md/suppress`; existing `/api/annotations` now writes through to the new format. FunctionNode warning badges are now clickable: opens a per-warning dismiss panel that POSTs to `/api/ryngo-md/suppress`. New MCP tool `read_ryngo_md` exposes the parsed manifest to connected agents. Fixed landing-page wildcard route swallowing `/api/*` GETs; sub-class member line numbers + hover-to-emit-source-line plumbing across CellNode/ClassNode/FunctionNode (`source-nav.js`) so the inspector follows the cursor while reviewing warnings.
2026-05-10  claude  Compiler warnings on every function — `mvp/lib/warnings.js` runs heuristic O(n²)/O(n³) nested-loop detection, I/O-in-loop (network or DB inside `for`/`while`), recursion without memoization, long functions, too many params, and deep nesting. Each function carries `data.warnings`; FunctionNode renders a red/amber ⚠ badge with severity color + tooltip listing every warning. Tightened anomaly-detector thresholds (12× / 6× median + absolute floors 100 / 40) so big repos like zod and hono no longer trip 375 false positives.
2026-05-10  claude  Removed the MAX_FILES = 1500 hard cap. Compilation is deterministic regex / tree-walk; even 50k-file monorepos finish in seconds. Per-file size + per-file def-count caps stay (single 50 MB generated bundle would blow the parser; 5000-def file is unrenderable). Clone timeout bumped 60 s → 180 s for very large repos. Landing FAQ updated; new entry "Does it warn me about expensive code?"
2026-05-10  codex  Reliable beta hardening — shared analysis cache/preflight, rate limits, global clone cap, DB/storage health probes, and `quality:report`
2026-05-10  claude  Phase 4.5.2 — landing page now leads with a live embedded demo iframe of `/app/?repo=karpathy/autoresearch&embed=1`. New `?repo=` URL param auto-analyzes on mount; `?embed=1` toggles `<html data-embed>` so styles.css hides header / view-tabs / banners. Hero widened to 1240 px to fit the demo card; Safari-style traffic-light chrome above the iframe with an "open in new tab" link.
2026-05-10  claude  Phase 4.3 — keyboard shortcuts + `?` help overlay. New `mvp/src/components/HelpOverlay.jsx`; global keydown router in App.jsx with `Cmd+K` (fuzzy find), `Cmd+/` (theme toggle), `?` (help), `g g/d/h` chord (graph/dashboard/home), `L`/`F` (Layers/Files), Esc to close. New `?` button in the header next to the theme toggle.
2026-05-10  claude  Three more framework adapters — Spring (`@GetMapping/@PostMapping/...` + `@Entity` → 19 routes on `spring-petclinic`), SQLAlchemy (declarative-base subclasses → 16 models on `tiangolo/sqlmodel`), Terraform (`resource` / `data` / `module` blocks → 121 `infra-resource` nodes on `terraform-aws-vpc`). Three new classifications + sentinel repos with min-expects.
2026-05-10  claude  Rails adapter — `config/routes.rb` route extraction (verb DSL + `resources` / `resource` expansion to RESTful 7 / 6) + ActiveRecord-style `db-model` nodes from `app/models/**/*.rb`; verified 24 routes + 17 models on `rails/rails`. Adapter scans Ruby as `scanUnparsed` since the Ruby parser is still a stub.
2026-05-10  claude  Phase 6.2 — anomaly badges (`mvp/test/anomaly.js`). Flags per-repo classification counts that are >5× the corpus median (🔴 hard) or >3× (🟡 soft). Surfaced in stdout + a new section in `latest.md`. Doesn't fail the run — informational, paired with the Phase 6.3 expects gate.
2026-05-10  claude  feat(viewer): code panel — lightweight per-line syntax highlighting (TS/JS/Py: keywords, strings, comments, numbers, types, decorators) tied to existing palette tokens; theme-aware bg/colors (was hardcoded dark); polished focus-line + scrollbar; layer-view file chips bumped readability (12 px tag, hover border)
2026-05-10  codex  Compiler-quality reports — per-file parse status/backend, MCP `get_compile_report`, `/api/compile-report`, and enriched deployment health checks
2026-05-10  claude  feat(viewer): focus-view layout polish — hub re-centers when only one side has satellites; collapsed-height grid; column count derived from focus-canvas aspect (55 % of window); card shadows on all typed nodes + layers; bolder hover lift; focus-graph edges thicker for legibility when 20+ lines fan out from one hub
2026-05-10  claude  feat(viewer): 1 s smooth fade between light and dark theme (`html.theming` class added on toggle, removed ~1.1 s later, scoped CSS so hover stays snappy outside the toggle window)
2026-05-10  claude  Phase 4.5.1 — landing page at `/` with how-to-use steps + MCP install instructions (Claude Code, Claude Desktop, ChatGPT Apps SDK, self-host); SPA moved to `/app`; Ryngo palette + dark via `prefers-color-scheme`
2026-05-10  claude  Move Dockerfile + `.dockerignore` to repo root with `mvp/`-scoped paths (matches the existing Cloud Build trigger; legacy Plinth corpus excluded from build context)
2026-05-10  claude  GraphQL adapter — SDL `type/input/interface/enum/union/scalar` declarations from `.graphql` files + Query/Mutation/Subscription resolver maps from JS/TS; activates on 3 corpus repos (graphql-js: 575 types, nestjs, prisma-examples)
2026-05-10  claude  fix(corpus): `--filter` runs no longer write history.json or latest.md (their tiny totals were poisoning delta math on subsequent full runs)
2026-05-10  claude  Django adapter — urls.py routes (path / re_path / url) + db-models from `models.Model` subclasses; verified 11 routes + 7 models on `wagtail/bakerydemo`
2026-05-10  claude  Phase 6.4 — corpus CI workflow at `.github/workflows/corpus.yml` (PR-blocking on hard min violations; runs on PR + push to main)
2026-05-10  claude  Phase 6.3 — per-repo `expects` blocks (corpus harness exits non-zero on hard min violations; 13 sentinel repos seeded)
